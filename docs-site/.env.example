# Docs chat endpoint. In production this defaults to /api/chat
# (same-origin Worker Route). For local dev point it at the
# deployed workers.dev URL so `pnpm dev` can talk to the Worker
# without setting up a local route.
#
# NEXT_PUBLIC_CHAT_ENDPOINT=https://speckit-security-chat.christiank.workers.dev/api/chat
